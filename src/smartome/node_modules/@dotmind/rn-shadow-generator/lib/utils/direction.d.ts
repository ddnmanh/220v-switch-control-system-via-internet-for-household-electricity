import { DirectionType } from '../types';
declare const adjustShadowOffsetOnDirection: (level: number, direction: DirectionType) => {
    width: number;
    height: number;
};
export { adjustShadowOffsetOnDirection, };
